import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  statsLabel: {
    color: '#ffff',
    fontSize: 16,
    fontWeight: '600',
    marginTop: 5,
  },
});

export default styles;
